apiVersion: v1
kind: Service
metadata:
  name: {{ include "premonition.fullname" . }}
  labels:
    {{- include "premonition.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "premonition.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
