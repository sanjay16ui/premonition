apiVersion: v1
kind: Secret
metadata:
  name: premonition-secrets
  namespace: premonition
type: Opaque
stringData:
  # Replace before deploy — use external secrets manager in production
  PREMONITION_JWT_SECRET: "CHANGE-ME-generate-long-random-secret"
  PREMONITION_API_KEY: ""
